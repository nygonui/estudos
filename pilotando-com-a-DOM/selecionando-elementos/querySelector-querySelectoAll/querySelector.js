//querySelector()

const element = document.querySelector('.one')
console.log(element);

//ele busca o primeiro elemento que tem o parâmentro passado na função
//os parâmetros são os mesmo que usamos para selecionar alguma coisa no CSS