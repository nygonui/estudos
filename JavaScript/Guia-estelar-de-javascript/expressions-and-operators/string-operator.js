// Operador de Strinf (String Operator)

// comparison (comparação)
//console.log('a' == 'a')

// concatenation (concatenação)
// Retorna a união de duas strings

let alpha = 'alpha'
alpha += 'bet'
console.log(alpha)