// Manipulando Arrays 

// Criar Array com constructor

let myArray = new Array('arroz', 'feijão', 'macarrão')
console.log(myArray)

// a função construtora Array já existe no JS, por isso não foi necessário cria-la 